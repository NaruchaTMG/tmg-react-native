import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  buttonContainer: {
    width: "100%",
    backgroundColor: 'red',
    height: 50,
    borderRadius: 25,
    justifyContent: "center",
    alignItems: "center",
    borderColor: "#D7D7D7",
    borderWidth: 1
  }
})
